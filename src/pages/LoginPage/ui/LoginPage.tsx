import { Box } from "@chakra-ui/react"
import { Auth } from "@widgets/Auth"

const LoginPage = () => {
    return (
        <Box>
            <Auth/>   
        </Box>
    )
}

export default LoginPage